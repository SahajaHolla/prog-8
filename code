$ git init
$ git branch
* master
$ git checkout -b source
$ vi p8.txt
$ touch p8.txt
$ git add p8.txt
$ git commit -m"initial commit"
$ git checkout master
Switched to branch 'master'
$ git branch
* master
  source
$ git cherry-pick source
$ git log
$ git log --oneline
85b4544 (HEAD -> master) initial commit
4717932 (tag: v2.0) 7 prog
